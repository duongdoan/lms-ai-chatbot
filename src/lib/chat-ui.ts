/** Nội dung UI chat theo profile (đồng bộ với `buildProfileChatUi`). */
export type ProfileChatUi = {
  headerTitle: string;
  headerSubtitle: string;
  headerBadgeLetter: string;
  welcomeMessage: string;
  starterQuestions: string[];
};
